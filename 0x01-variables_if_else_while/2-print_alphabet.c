#include <stdio.h>

/**
 * main - Entry point
 * this code displays last digit of a rando number
 * Return: always 0 (Success)
 */
int main(void)
{
	for (int alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);
	}
	putchar('\n');
	return (0);
}
