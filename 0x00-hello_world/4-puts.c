#include<stdio.h>
/**
 * main-entry code
 * This code uses puts to display data
 * Return:always 0 since void takes no argument
 */
int main(void)
{
	char s[] = "Programming is like building a multilingual puzzle";

	puts(s);
	return (0);
}
